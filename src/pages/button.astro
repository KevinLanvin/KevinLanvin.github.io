<div class='test'>
  <button class='button'> coucou</button>
</div>

<style lang='scss'>
  .test {
    background: url('/images/bg-purple.jpg');
    padding: 10rem 3rem;
  }
  .button {
    color: white;
    position: relative; /* or absolute */
    font-size: 5rem;
    background: #FFFFFF0A;
    border: 1px solid white;
    border-radius: 5rem;
    padding: 2rem;
    backdrop-filter: blur(5px);
    &::before {
      content: "";
      position: absolute;
      top: 0;
      left: 5rem;
      width: calc(50% - 5rem);
      height: 10px;
      background: radial-gradient(farthest-side, #ffffff, #ffffff00);
      transform: translate(0, -55%);
      transition: transform 0.2s;
    }
    &::after {
      content: "";
      position: absolute;
      bottom: 0;
      right: 5rem;
      width: calc(50% - 5rem);
      height: 10px;
      background: radial-gradient(farthest-side, #ffffff, #ffffff00);
      transform: translate(0, 55%);
      transition: transform 0.2s;
    }
    &:hover {
      &::before {
        transform: translate(100%, -55%);
      }
      &::after {
        transform: translate(-100%, 55%);
      }
    }
  }
</style>
