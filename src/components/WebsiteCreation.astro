---
import SquareHammer from "./SquareHammer.astro";
---

<section class='websiteCreation'>
  <h2 class='subtitle'>Création de sites web</h2>
  <p>
    Je réalise des <strong>sites web sur mesure</strong>, faciles à prendre en
    main.<br />Vous pourrez choisir entre <strong>un site statique</strong> pour
    les petis budgets ou <strong>un site clé en main</strong> qui vous permettra
    de créer et administrer vous-mêmes votre contenu.<br />Tous mes sites sont <a
      href='creation-site-web#ecoconception'>éco-conçus</a
    >, optimisés pour mobiles, accessibles et performants.
  </p>
  <div class='bigSquare'>
    <SquareHammer class='squareHammer' />
    <svg
      class='circle'
      viewBox='0 0 800 800'
      fill='none'
      xmlns='http://www.w3.org/2000/svg'
    >
      <defs>
        <radialGradient id='circleGradient' cx='0' cy='50%' r='25%'>
          <stop offset='0%' stop-color='#ffffff'></stop>
          <stop offset='100%' stop-color='#ffffff00'></stop>
        </radialGradient>
      </defs>
      <circle
        cx='400'
        cy='400'
        r='396'
        stroke-width='4'
        stroke='url(#circleGradient)'></circle>
    </svg>
    <a href='creation-site-web' class='services'>Découvrir mes services </a>
  </div>
</section>

<style lang='scss'>
  .websiteCreation {
    background-color: #ccd6e8;
    box-shadow: 0 150px 150px 150px #cdd6e8;
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 10rem;
    padding: 0 12%;
    z-index: 1;
    h2 {
      top: 0;
      margin-left: -16%;
    }
    p {
      color: #4f4f4f;
      position: absolute;
      top: 20rem;
      right: 12%;
      width: calc(100% - 45rem);
      max-width: 50rem;
    }
    :global(.squareHammer) {
      z-index: 2;
    }
    .bigSquare {
      position: relative;
      width: 45.24rem;
      height: 45.24rem;
      left: -16%;
      transform: translateX(-25%);
      .circle {
        width: 45rem;
        height: 45rem;
        position: absolute;
        left: 0;
        top: 30%;
        z-index: -1;
        animation: rotateCircle 4s linear infinite;
      }
      .services {
        position: absolute;
        width: max-content;
        left: 100%;
        top: 85%;
        font-weight: bold;
        color: #4f4f4f;
        background: #4f4f4f0a;
        border: solid 2px #4f4f4f;
        border-radius: 5rem;
        padding: 1rem;
        backdrop-filter: blur(4px);
        text-decoration: none;
        cursor: pointer;
        &::after {
          content: "";
          background: url("/images/servicesArrow.svg");
          background-size: contain;
          background-repeat: no-repeat;
          width: 200px;
          height: 100px;
          position: absolute;
          bottom: 100%;
          right: 65%;
        }
      }
    }
  }
  @keyframes rotateCircle {
    0% {
      transform: rotate(0deg);
    }
    50% {
      transform: rotate(360deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  @media screen and (max-width: 600px) {
    .websiteCreation {
      display: flex;
      flex-direction: column;
      gap: 2rem;
      padding: 0 1rem;
      padding-bottom: 15rem;
      h2 {
        margin-top: 5rem;
        margin-left: -1rem;
      }
      p {
        position: static;
        margin: 2rem 0;
        padding: 0 1rem;
        width: 100%;
      }
      :global(.squareHammer) {
        z-index: 2;
      }
      .bigSquare {
        width: 25rem;
        height: 25rem;
        .circle {
          width: 25rem;
          height: 25rem;
        }
        .services {
          top: 150%;
          left: 50%;
          &::after {
            width: 150px;
            height: 75px;
            right: 20%;
            transform-origin: bottom right;
            transform: rotate(35deg);
          }
        }
      }
    }
  }
</style>
