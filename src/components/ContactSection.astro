---
import BorderedButton from "./BorderedButton.astro";
---

<section class='contactSection'>
  <div class='fog'></div>
  <h2>
    Et si on discutait de <strong>vos projets</strong><br /><em>ensemble ?</em>
  </h2>
  <BorderedButton addedClasses="contactButton" link="mailto:info.lanvink@gmail.com">
    Contactez-moi !
  </BorderedButton>
  <div class='shadow'></div>
</section>

<style lang='scss'>
  .contactSection {
    background: url("/images/ecosse-background.webp");
    background-size: cover;
    position: relative;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 5rem;
    align-items: center;
    text-align: center;
    overflow: hidden;
    &::before {
      content: "";
      width: 100%;
      height: 100%;
      position: absolute;
      top: 0;
      left: 0;
      background: url("/images/train.webp");
      background-size: cover;
      z-index: 1;
    }
    h2 {
      color: white;
      font-size: 4rem;
      line-height: 6rem;
      font-weight: normal;
      z-index: 2;
      strong {
        font-weight: normal;
        font-style: italic;
      }
      em {
        font-size: 6rem;
        font-weight: bold;
      }
    }
    .shadow {
      width: 200vw;
      height: 100vh;
      z-index: 1;
      position: absolute;
      transform: translateX(-50%);
      background: radial-gradient(
        ellipse at bottom,
        #000000 0%,
        #000000f0 20%,
        #00000070 50%,
        #00000050 60%,
        transparent 100%
      );
      bottom: 0;
      left: 50%;
    }
    .fog {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 50%;
      &::after {
        content: "";
        background-image: url("/images/fog1large.webp");
        background-repeat: repeat-x;
        background-position-y: bottom;
        width: 5000px;
        height: 100%;
        position: absolute;
        top: 0;
        left: 0;
        animation: fog1movement 10s linear infinite reverse;
      }
      &::before {
        content: "";
        background-image: url("/images/fog1large.webp");
        background-repeat: repeat-x;
        background-position-y: bottom;
        width: 5000px;
        height: 100%;
        position: absolute;
        top: 0;
        left: 0;
        animation: fog1movement 20s linear infinite reverse;
      }
    }
  }
  :global(.contactButton) {
    z-index: 2;
  }

  @media screen and (max-width: 600px) {
    .contactSection {
      padding: 0 1rem;
      padding-top: 10rem;
      gap: 2rem;
      justify-content: flex-start;
    background: url("/images/ecosse-background-mobile.webp");
      h2 {
        font-size: 2rem;
        line-height: 3rem;
        em {
          font-size: 3rem;
        }
      }
    }
  }
</style>
