<section id='hero'>
  <h1 id='title'>
    Développeur web <em>fullstack</em>, <strong>en freelance</strong> à Lille
  </h1>
  <div class='contactWrapper' onclick="document.getElementById('websiteCreation').scrollIntoView()">
    <div class='contactAvatar'>
      <img src='/images/avatar.webp' alt='Kevin Lanvin' />
    </div>
    <span>Hello, moi c'est Kévin !</span>
  </div>
  <div class="nextSectionGradient"></div>
</section>

<style lang='scss'>
  #hero {
    padding: 10em 0;
    height: calc(100vh - 165px);
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    min-height: 100vh;
    padding-top: 165px;
    overflow-y: visible;
    background-image: url("/images/bg.webp");
    background-color: #000;
    background-size: cover;
    background-position: top center;
    background-repeat: no-repeat;
    z-index: 2;
    &::after {
      content: "";
      background-image: url("/images/fog1large.webp");
      background-repeat: repeat-x;
      width: 5000px;
      height: 100%;
      position: absolute;
      top: 0;
      left: 0;
      animation: fog1movement 10s linear infinite forwards;
    }
    &::before {
      content: "";
      background-image: url("/images/fog1large.webp");
      background-repeat: repeat-x;
      width: 5000px;
      height: 100%;
      position: absolute;
      top: 0;
      left: 0;
      animation: fog1movement 17s linear infinite forwards;
    }
    .nextSectionGradient {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 150px;
      background: linear-gradient(to top, #cdd6e8, #cdd6e800);
      z-index: 1;
    }
  }

  @keyframes rotate {
    0% {
      transform: translate(-50%, -50%) rotate(0deg);
    }
    100% {
      transform: translate(-50%, -50%) rotate(360deg);
    }
  }

  @keyframes float {
    0% {
      transform: transtaleY(0%);
    }
    100% {
      transform: translateY(-50%);
    }
  }

  @keyframes fog1movement {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-984px);
    }
  }

  .contactWrapper {
    position: absolute;
    bottom: 0;
    right: 20%;
    width: 10rem;
    height: 10rem;
    z-index: 2;
    cursor: pointer;
    .contactAvatar {
      width: 100%;
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
      border: 2px solid white;
      border-radius: 50%;
      background: linear-gradient(
        225deg,
        rgba(255, 255, 255, 0.7) 1.22%,
        rgba(255, 255, 255, 0) 100%
      );
      transform: translateY(20%);
      animation: floatContact 2s ease-in-out infinite alternate;
    }
    img {
      width: 80%;
      height: 80%;
      object-fit: contain;
    }
    span {
      position: absolute;
      color: white;
      top: -6rem;
      left: -3rem;
      font-size: 1rem;
      width: max-content;
      transform: rotate(-3deg);
      text-shadow: 0px 0px 20px #000000;
    }
    &::after {
      content: "";
      background: url("/images/avatarArrow.svg");
      background-size: contain;
      background-repeat: no-repeat;
      background-position: center bottom;
      position: absolute;
      bottom: 80%;
      left: 33%;
      height: 5rem;
      width: 5rem;
    }
  }

  @keyframes floatContact {
    from {
      transform: translateY(20%);
    }
    to {
      transform: translateY(35%);
    }
  }

  @media screen and (max-width: 600px) {
    #hero {
      background-position-x: center;
      background-image: url("/images/bg-mobile.webp");
      padding: 0 1rem;
      height: calc(100vh - 80px);
      max-height: calc(100vh - 80px);
      .contactWrapper {
        width: 8rem;
        height: 8rem;
        span {
          font-size: 1rem;
          top: -3rem;
          right: 2rem;
          left: unset;
        }
        &::after {
          height: 5rem;
          width: 5rem;
          bottom: 35%;
          right: -10%;
          left: unset;
          transform-origin: top left;
          transform: rotate(-25deg);
        }
      }
    }
  }
</style>
